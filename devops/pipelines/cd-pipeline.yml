trigger: none

pool:
  vmImage: ubuntu-latest

stages:
  - stage: Deploy
    jobs:
      - job: DeployApiAndFrontend
        steps:
          - script: echo "Deploy WMS API and Angular frontend"
            displayName: Deployment placeholder
